{
  "name": "voiceish-voipms-standalone-webapp",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free Voice-ish reseller client for SMS, MMS, and VoxVolley.",
  "scripts": { "start": "node server.mjs", "check": "node --check server.mjs && node --check web-chrome-shim.js && node --check background.js && node --check popup.js && node --check sw.js" },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
